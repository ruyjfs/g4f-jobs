export type DictionaryType = {
  layout: {
    button: {
      search: string;
      add: string;
    };
  };
  form: {
    input: {
      personas: {
        label: string;
      };
      projects: {
        label: string;
      };
      activities: {
        label: string;
      };
    };
  };
  display: {
    myActivities: string;
    projects: string;
    activities: string;
    logout: string;
  };
};
